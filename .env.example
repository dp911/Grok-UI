# Loopback is the safest default.
HOST=127.0.0.1
PORT=4310

# Optional overrides.
# GROK_HOME=/Users/you/.grok
# GROK_BIN=/absolute/path/to/grok

# Required when HOST is not 127.0.0.1, localhost, or ::1.
# Generate with: openssl rand -base64 32
# GROK_UI_TOKEN=replace-me
