# Loopback is the safest default.
HOST=localhost
PORT=4310

# Optional overrides.
# GROK_HOME=<grok-state-directory>
# GROK_BIN=<path-to-grok>
# GROK_UI_STATE_DIR=<dashboard-state-directory>

# Required when HOST is not localhost.
# Generate with: openssl rand -base64 32
# GROK_UI_TOKEN=replace-me
